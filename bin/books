#! /usr/bin/env bb

(require '[babashka.deps :as deps])

(deps/add-deps
 '{:deps {hickory/hickory   {:mvn/version "0.7.1"}}})

(require '[hickory.core :as h])

(def parsed-doc (h/parse "<a href=\"foo\">foo</a>"))

(h/has-hickory parsed-doc)












